using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Q_small_bugs : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}


// kod gdzie jeśli postać użyje Q 15 dystansu od enemy tag obiektu bug wykonuje animacje umierania 1 raz i obiekt przestaje istnieć